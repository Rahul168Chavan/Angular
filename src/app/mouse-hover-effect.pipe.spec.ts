import { MouseHoverEffectPipe } from './mouse-hover-effect.pipe';

describe('MouseHoverEffectPipe', () => {
  it('create an instance', () => {
    const pipe = new MouseHoverEffectPipe();
    expect(pipe).toBeTruthy();
  });
});
